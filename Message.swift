class Message {
    var title: String
    var body: String
    var dateInterval: Date
    init(title: String, body: String, dateInterval: Date) {
        self.title = title
        self.body = body
        self.dateInterval = dateInterval
    }
}
